# Cold Email Manager

A comprehensive cold email campaign manager with audience scheduling, A/B testing, template management, and analytics.

## Run Locally

**Prerequisites:** Node.js

1. Install dependencies:
   `npm install`
2. Set up your `.env` file based on `.env.example` with your Firebase credentials.
3. Run the app:
   `npm run dev`
